---
name: gzh-deepsea-design
description: 微信公众号文章排版引擎（「深海橙光」专属风格），将 Markdown 转换为可直接粘贴到公众号编辑器的 HTML。亮橙（#F0691C）为主色、深黑文字 + 提亮蓝（#245688）做标签/英文小字点缀的编辑风：橙色编号/下划线/竖条/标签、白卡引言卡（橙竖条+橙引号）+ 深黑卡签名区首尾呼应（无投影）、大编号章节、目录导航、代码块、图片/GIF。支持 Markdown / Word(.docx) / PDF / 纯文本输入（非 Markdown 先自动归一化），也支持"一键自动排版"。触发场景：(1) 用户提到"公众号排版""公众号文章""微信排版"且用本风格，(2) 用户想把文章（md/docx/pdf/纯文本）转成"橙光/暖橙风"公众号 HTML，(3) 用户说"自动排版""一键排版"公众号内容。不用于生成普通网页/落地页/PPT（用前端或 PPT 类 skill）；需要多风格自选时不适用本 skill（本 skill 只有一组深海橙光主题）。
---

# 公众号排版 Skill —— 深海橙光

把一篇 Markdown 文章转换为可直接复制粘贴进微信公众号编辑器、且粘贴后样式不丢失的 HTML。

本 skill 只内置一套主题：**深海橙光**（亮橙 `#F0691C` 为主色，深黑文字 + 提亮蓝 `#245688` 点缀）。具体 HTML 代码一律从 `references/theme-deepsea-orange.md`（主题组件库）+ `references/common-components.md`（通用增量库：代码块 / 图片·GIF / 小标签标题）**取组件，不要凭记忆手写**。

## 工作流

### 0. 输入与格式归一化

用户可能给：Markdown 文本或 `.md` 路径（直接进第 1 步）、`.docx`、`.pdf`、`.txt`/无标记纯文本、网页富文本。**非 Markdown 输入必须先读 [references/format-normalize.md](references/format-normalize.md) 按其规则转成 Markdown 草稿并做结构确认**（docx 用 `scripts/extract_docx.py`，PDF 用 Read 分页读取+清噪，纯文本按标题启发式推断结构）。什么都没给时，向用户索要。

用户说「直接排 / 自动排 / 一键 / 不用问」时进**全自动模式**：跳过结构确认与提问，自动推断结构、按题材排版校验，交付时附决策说明（章节结构、自拟标题、橙色锚点选择理由）。

### 1. 固定主题

本 skill 只有一个常驻主题「深海橙光」，**无需选主题**。直接读 [references/theme-deepsea-orange.md](references/theme-deepsea-orange.md) 开始。

### 2. 读组件库 + 个性化配置（三份）

(1) Read `references/theme-deepsea-orange.md`——引言卡、章节标题、正文标记、签名等主题专属组件。
(2) **同时 Read 通用增量库** `references/common-components.md`——代码块、图片/GIF、小标签标题共用；套用时按主题"设计变量速查表"把示例红色值换成对应色（主色 `#F0691C`、浅底 `#FFF3E8`、深字 `#1C1F24`）。
(3) **同时 Read 个性化配置** `references/persona.md`——署名/落款/一句话简介、猫 IP 头像的出现位置与外链约束。装配时把 `{{作者名}}`/`{{简介}}`/`{{互动引导文案}}` 替换成 persona.md 里的值。

### 3. 解析 Markdown 结构

| 元素 | 识别规则 |
|------|---------|
| 文章标题 | `# 标题` 或 frontmatter `title` |
| 开头引言 | 文章最开头的 `> 引用` 块 |
| 章节标题 | `## 标题` |
| 子章节 | `### 标题` |
| 加粗 / 高亮 / 下划线 | `**文字**` / `==文字==` / `<u>文字</u>` 或 `++文字++` |
| 引用段落 | 非开头的 `> 文字` |
| 图片 / GIF | `![说明](URL)`、`![](xxx.gif)`（GIF 与图片同样处理） |
| 代码 / 命令 / Prompt | ` ``` 围栏代码块 ``` `、行内 `` `code` `` |
| 分割线 / 列表 | `---` / `- 项` 或 `1. 项` |
| 表格 | `\|` 分隔的 Markdown 表格 → 用主题库表格组件 |

**解析完结构后，判定文章类型**（取主导类型，可复合）：教程/操作指南、盘点/工具清单、观点/深度分析、访谈/人物特稿、数据复盘/报告、生活/情感随笔、案例实战。判定依据：步骤和命令多→教程；并列条目多→盘点；引语和人物叙事多→访谈；数字和对比多→数据；论证推演多→观点。

### 4. 按配方选组件组合 → 装配 HTML

**先查主题库的「文章类型 → 组件组合配方」表**，按文章类型确定核心组件组合与点缀组件——不要拿到组件库就逐段随机选组件，配方保证同类文章排版气质稳定。配方之外的元素再按主题库映射规则表补充。

然后依主题库的**"完整文章模板骨架"章节**装配，把每个 Markdown 元素替换为对应组件：

- **骨架顺序以主题库为准**，不要自行改序。
- **行内标记按语义到组件库里找对应组件**：`**加粗**`→深黑加粗（7a）；`==高亮==`→浅橙底标签（7b）；`<u>文字</u>` 或 `++文字++`→橙色下划线（7d）；`~~文字~~`→删除线；`>引用`→引用高亮块（8x）。
- 来自**通用增量库**的组件：` ``` 代码块 ``` `→ 1a 深色 / 1b 浅色代码块（1b 左竖条换 `#F0691C`），行内 `` `code` ``→ 1c（文字色换 `#F0691C`）；`![说明](url)`→ 2a 图片（有说明才加说明）、`.gif`→ 2b；待补素材占位→ 2c 居中板块；需要突出的小标题/强调→ 3a 左竖条 / 3b 药丸（主色换 `#F0691C`）。**优先级：先查主题库映射规则表**——有等价语义组件就用主题库版本。
- **强调用小标签/左竖条，不要用四周虚线框（dashed）**。
- **配色角色**：橙色是主旋律（下划线 / 章节编号 / 竖条 / 标签实底 / 分割线圆点 / STEP 标签高频用）；**深黑（`#1C1F24`）负责标题与金句的重字**。引言卡是白卡（橙竖条+橙引号）、签名区是深黑卡（橙边条）。**最强内联锚点**（深黑实底白字 7f）全篇 ≤3 处，避免满屏橙发闷。

### 5. 校验合规（强制）

把生成的 HTML 写入目标文件后，**必须运行校验脚本**，ERROR 清零才算完成：

```bash
# 用脚本所在 skill 的绝对路径调用，HTML 参数也用其实际路径
/Users/lex/.codex/skills/gzh-deepsea-design/scripts/validate_gzh_html.py <生成的.html 的实际路径>
```

它确定性地检查平台禁用项和 `<span leaf>` 包裹。报 ERROR 就回到第 4 步修；**半角标点 WARNING 同样要修复到 0 再交付**。

### 6. 输出

**产物格式：纯 `<section>…</section>` 正文片段，从全局容器开始，不要包 `<!DOCTYPE>`/`<html>`/`<head>`/`<body>`**。

1. **干净正文文件**：HTML 保存到当前工作目录，文件名 `{原文件名}_排版_深海橙光(deepsea-orange).html`。
2. **带「复制」按钮的预览页**：
   ```bash
   /Users/lex/.codex/skills/gzh-deepsea-design/scripts/wrap_preview.py <上面的干净正文.html>
   ```
   产出 `{...}_预览.html`——浏览器打开后右上角有「复制到公众号」按钮。
3. 告知用户：**打开 `{...}_预览.html` → 点右上角「复制」→ 公众号编辑器粘贴**；给出干净正文文件路径兜底，附校验结论。

## 生成时的智能处理

1. **章节自动编号**：按 `##` 出现顺序分配 `01/02/03…`；末章若为结语/总结类，用 `∞` + `THE END`。
2. **英文标签**：据中文章节标题生成英文标签（实测→TEST、教程→TUTORIAL、总结→SUMMARY、思考→THOUGHTS…）。
3. **正文关键词下划线（核心特色）**：对**每个正文段落**主动找出 1–3 个最重要的短语，用橙色下划线（7d）标记。优先标核心观点、结论、关键数据、专有名词；短语 4–15 字；整段无要点可不标。
4. **引言关键词点睛**：识别开头金句里的核心词，用橙色下划线点睛。
5. **目录提取**：从所有 `##` 取**精选 3 个核心看点**作为导读（不是完整章节列表）。
6. **开头引言卡署名**：按 [references/persona.md](references/persona.md) 的署名值填"作者名"；文章若自带署名则用文章署名。**不要写死别人名字**。
7. **尾部作者签名区（仅末尾一处）**：署名/简介/互动文案取自 [references/persona.md](references/persona.md)，装配时替换占位符（用户请求里给了署名就临时覆盖）。原文末尾已有签名段 → 直接沿用，不再生成第二个。
8. **列表转换**：按主题库映射规则处理。
9. **中文全角标点**：正文标点一律用全角（，。！？：；""''（）—— …）。**生成 HTML 时就直接写弯引号""''**。例外：代码块、行内代码、英文专名/URL/代码标识符内部保持原样。

## 猫 IP 集成（个人标识，可选装饰）

用户有一只**戴红针织帽、穿蓝羽绒**的猫作个人 IP（红帽≈主橙、蓝衣≈辅助蓝，天然贴合配色），本地源图在 `assets/cat-ip.png`。出现位置与克制原则（全文 ≤2 处）见 [references/persona.md](references/persona.md)。

- 用户**提供了猫图外链 URL**（http/https）→ 签名区用**组件 16b**（把 URL 填进 `{{猫图URL}}`）；**引言卡保持组件 2，不加头像**。
- 用户**没提供外链** → 签名区用组件 16（**不要用本地 `assets/cat-ip.png` 硬塞 `<img>`，公众号会破图**），交付时提示用户：把猫图传到图床/公众号素材库后把链接发我，我再加 IP 头像。
- 猫头像**只出现在尾部签名区**（开头引言卡不放），统一圆形 + 白描边 + 橙外圈。

## 封面 / 配图生图（可选）


当用户要求"给文章配一张封面图 / 生成一张主题图 / 引言卡背景图"时，**调用 `qiyuan-image` skill**（`node ~/.codex/skills/qiyuan-image/qiyuan-image.mjs generate --prompt ... --out cover.png --size 1024x1024`）生成位图。封面图 prompt 统一按本主题气质写：

> 暖橙（#F0691C 系，偏沉稳的橙）为主的现代编辑风封面，大面积橙色渐变/几何色块，点缀深黑（#1C1F24）线条或块面做对比，高级、有活力、留白，无文字（文字由公众号标题承担），适合科技/观点类文章头图。

生成后把图片 URL/路径填进组件 14 图片容器；若公众号只吃外链，提醒用户把本地图先传到图床再替换 `src`。

## 视觉层级（3 层递进）

| 层级 | 作用 | 频率 | 手段 |
|------|------|------|------|
| 锚点层 | 最强锚点：产品名/步骤/CTA/核心金句 | 内联锚点 ≤3 处 | 深黑实底白字 7f、引言卡·签名区大色块 |
| 标记层 | 正文关键词，每段 1–3 处 | 高频 | 橙色下划线 7d + 深黑加粗 |
| 容器层 | 引用块、概念标签、长句强调 | 按需 | 浅橙底引用、荧光笔、徽章 |

## 平台红线（核心，完整检查交给校验脚本）

- **禁止**：`<style>`/`<script>`/`<div>`、`class`/`id` 属性、`position:fixed/absolute/sticky`、`float`、`@media`/`@keyframes`、`display:grid`、CSS 变量、外部字体/CSS。
- **必须**：样式全部内联 `style`；所有文字节点用 `<span leaf="">文字</span>` 包裹（否则粘贴后样式丢失）。
- **可用**：`display:flex`（有限）、`linear-gradient`、`border-radius`、`box-shadow`、`<section>/<p>/<span>/<strong>/<img>/<h3>`。

## Gotchas（真实排版踩过的坑）

- **漏 `<span leaf>` 包裹**是最常见致命错——粘贴到公众号后样式整片丢失。靠第 5 步校验脚本兜底，别跳过。
- **下划线：逐段落实、每段 1–3 个短语**。不要整段划线，也不要有的段标有的段漏；列表项里的关键描述同样要标。
- **章节编号错乱**：严格按 `##` 顺序，不跳号；`∞` 只用于末章。
- **签名区有且仅有末尾一个**；原文末尾已有签名段就并入，不要保留原文段又再生成。
- **图片说明硬造**：只有 `![说明](url)` 里真有说明文字才生成说明组件；空 alt 不编造。
- **图片自适应、不铺满**：`<img>` 一律 `max-width:100%;height:auto;display:block;margin:0 auto`；只有表格/封面卡/流程图这类布局元素才用 `width:100%`。
- **锚点层滥用**：最强强调全文 ≤ 5 处、亮橙 ≤3 处，到处加粗等于没有重点。
- **原文内容遗漏**：每个段落、每张图都要转换，不得漏；不自行增删实质内容。
- **占位图残留**：组件里带图片占位的，没有真实 URL 时整行删掉。
- **目录是精选不是全量**：导读展示精选 3 个核心看点。
- **不用虚线框**：突出标题/强调用小标签或左竖条，不要 `border:…dashed` 包标题。**例外**：通用库 2c 待补素材占位是唯一允许虚线的场景。
- **标点别混半角**：正文半角逗号句号、英文直引号都改全角；代码/行内代码内半角保持原样。
- **代码/Prompt 必须用代码块**（通用库 1a/1b），不要塞进普通段落或引用块。
- **代码块要紧凑、忌大空白**：每行一个 `<p style="margin:0">`，**绝不用 `white-space:pre`**；缩进只用全角空格 `　`，行距靠 `line-height:1.6`。
- **待补素材居中**：`【插入…】` 等占位用通用库 2c 居中板块。
